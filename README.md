
  # Update event page design

  This is a code bundle for Update event page design. The original project is available at https://www.figma.com/design/kz4fVq5ugclbFvQyX3qiZF/Update-event-page-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  